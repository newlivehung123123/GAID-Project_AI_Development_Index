# data artefacts (regenerated by the pipeline; manifest.json and
# data/reference/ ARE committed)
data/raw/
data/wave/
data/processed/*
!data/processed/w1_v2/
data/processed/w1_v2/*
# the published index is committed so it can be checked without a rebuild
!data/processed/w1_v2/indices.parquet
data/eval/

# regenerated by `site-data` at build time; frontend code IS committed
site/data/

# secrets
.env

# python
__pycache__/
*.pyc
.venv/
gaid-site-upload.zip

# macOS
.DS_Store

# duplicate upload folder + local paper build
gaid-site-upload 2/
paper/*.docx
